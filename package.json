{
  "scripts": {
    "test": "node --test web/*.test.js",
    "build": "rm -rf dist && mkdir -p dist && cp -R index.html web vendor opening.wav ending.wav _headers dist/"
  },
  "dependencies": {
    "@ffmpeg/core": "^0.12.10",
    "@ffmpeg/ffmpeg": "^0.12.10",
    "@ffmpeg/util": "^0.12.1"
  }
}
